import 'package:flutter/material.dart';

class AppColors {
  static const bg = Color(0xFF0E0F12);
  static const canvas = Color(0xFF15171B);
  static const panel = Color(0xFF1B1E24);

  static const text = Color.fromARGB(202, 237, 237, 237);
  static const muted = Color.fromARGB(176, 189, 189, 189);

  static const border = Color(0xFF2C3138);
  static const borderStrong = Color(0xFF3A4048);
  static const borderFocus = Color(0xFF5C6470);

  static const selection = Color(0x405C9EFF);
  static const handle = Color(0xFF5C9EFF);
}
