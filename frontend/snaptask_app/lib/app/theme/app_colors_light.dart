import 'package:flutter/material.dart';

class AppColorsLight {
  // Base
  static const bg = Color(0xFFFFFFFF);
  static const surface = Color(0xFFFFFFFF);

  // Texto
  static const text = Color(0xFF1B1F2A);
  static const muted = Color(0xFF5F6B7A);

  // UI
  static const border = Color(0xFFE1E6EF);
  static const accent = Color(0xFF2B6CF6);

  // Campos/seleção
  static const fieldFill = Color(0xFFF7F8FA);
  static const selection = Color(0x332B6CF6);
  static const handle = Color(0xFF2B6CF6);
}
